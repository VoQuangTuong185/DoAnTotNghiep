apiVersion: v1
kind: Service
metadata:
  name: mainservice-np-srv
spec:
  type: NodePort
  selector:
    app: mainservice
  ports:
    - name: mainservice
      protocol: TCP
      port: 80
      targetPort: 80